{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:digitalocean": "npm install --production=false && npm run build && npm ci"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.5.1"
    }
}
